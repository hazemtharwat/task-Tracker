name: Deploy Angular to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build Angular
        run: npx ng build --configuration production --base-href /task-Tracker/

      - name: Deploy
        run: npx angular-cli-ghpages --dir=dist/task-tracker/browser --no-silent
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}